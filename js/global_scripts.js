// This file is for any JavaScript that needs to run on all pages.
// For now, it can be empty or include a basic mobile menu toggle if you add one later.

// Example: Basic console log to confirm it's loaded
console.log("Jhoven's Tambayan Global Scripts Loaded - Astig!");

// Conceptual Mobile Menu Toggle (if you add a burger icon and nav links for mobile)
/*
const mobileMenuButton = document.querySelector('.mob-menu-button'); // Add this button to your header HTML
const navMenu = document.querySelector('header nav ul');

if (mobileMenuButton && navMenu) {
    mobileMenuButton.addEventListener('click', () => {
        navMenu.classList.toggle('active');
    });
}
*/
console.log("Jhoven's Tambayan Global Scripts Loaded - Astig!");

// Conceptual Mobile Menu Toggle (Uncomment and adapt if you add a mobile menu burger)
/*
document.addEventListener('DOMContentLoaded', () => {
    const mobileMenuButton = document.querySelector('.mobile-menu-trigger'); // You'd add this button in your HTML header
    const nav = document.querySelector('header nav');

    if (mobileMenuButton && nav) {
        mobileMenuButton.addEventListener('click', () => {
            nav.classList.toggle('active-mobile-nav');
            // Optional: Change burger icon to X
            mobileMenuButton.querySelector('i').classList.toggle('fa-bars');
            mobileMenuButton.querySelector('i').classList.toggle('fa-times');
        });
    }
});
*/